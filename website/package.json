{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "check:html": "html-validate 'dist/**/*.html'",
    "check:links": "node scripts/check-links.mjs",
    "verify": "npm run check && npm run build && npm run check:html && npm run check:links",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.4",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.6",
    "yaml": "^2.9.0"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "html-validate": "^11.10.0",
    "typescript": "^6.0.3"
  }
}
