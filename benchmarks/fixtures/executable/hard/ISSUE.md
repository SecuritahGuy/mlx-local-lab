With `DEBUG` unset, error payloads expose exception details. Trace configuration
and application logic, then make the default safe without breaking explicit
truthy values such as `1`, `true`, and `yes`.
